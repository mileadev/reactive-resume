name: autofix.ci

on:
  pull_request:
  push:
    branches: ["main"]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  autofix:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Check for merge conflict markers
        run: |
          if git grep -nEI '<{7} |>{7} |^={7}$' -- ':(exclude)*.md' ':(exclude)*.mdx'; then
            echo "::error::Merge conflict markers found in tracked files"
            exit 1
          fi

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "lts/*"
          cache: "pnpm"

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # Knip may return non-zero after applying the fixes it can make. Do not
      # prevent Biome/Markdownlint from producing their canonical fixes; the
      # strict Knip verification below still fails the workflow when issues
      # remain after the autofix pass.
      - name: Remove Unused Dependencies
        continue-on-error: true
        run: pnpm knip --fix

      - name: Lint and Format
        run: pnpm check

      - name: Autofix
        uses: autofix-ci/action@7a166d7532b277f34e16238930461bf77f9d7ed8

      - name: Verify No Unused Code or Dependencies Remain
        run: pnpm knip
